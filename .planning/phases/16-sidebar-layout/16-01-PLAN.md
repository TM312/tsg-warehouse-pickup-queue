---
phase: 16-sidebar-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - staff/app/components/ui/sidebar/
  - staff/app/components/ui/avatar/
  - staff/app/components/ui/dropdown-menu/
  - staff/app/components/ui/tooltip/
autonomous: true

must_haves:
  truths:
    - "shadcn-vue sidebar component is installed and importable"
    - "shadcn-vue avatar component is installed and importable"
    - "shadcn-vue dropdown-menu component is installed and importable"
    - "shadcn-vue tooltip component is installed and importable"
  artifacts:
    - path: "staff/app/components/ui/sidebar/index.ts"
      provides: "Sidebar component exports"
      exports: ["Sidebar", "SidebarContent", "SidebarFooter", "SidebarGroup", "SidebarGroupContent", "SidebarHeader", "SidebarInset", "SidebarMenu", "SidebarMenuButton", "SidebarMenuItem", "SidebarProvider", "SidebarRail", "SidebarTrigger", "useSidebar"]
    - path: "staff/app/components/ui/avatar/index.ts"
      provides: "Avatar component exports"
      exports: ["Avatar", "AvatarFallback", "AvatarImage"]
    - path: "staff/app/components/ui/dropdown-menu/index.ts"
      provides: "DropdownMenu component exports"
      exports: ["DropdownMenu", "DropdownMenuContent", "DropdownMenuItem", "DropdownMenuTrigger"]
    - path: "staff/app/components/ui/tooltip/index.ts"
      provides: "Tooltip component exports"
      exports: ["Tooltip", "TooltipContent", "TooltipProvider", "TooltipTrigger"]
  key_links: []
---

<objective>
Install shadcn-vue sidebar, avatar, dropdown-menu, and tooltip components for the sidebar layout implementation.

Purpose: These components provide the building blocks for the collapsible sidebar navigation. The sidebar component includes collapse state management, mobile drawer behavior, and icon-only mode. Avatar and dropdown-menu are used for the user section in the sidebar footer. Tooltip provides accessible labels in collapsed mode.

Output: Four new component directories in staff/app/components/ui/ with all sub-components and exports.
</objective>

<execution_context>
@/Users/thomas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thomas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sidebar-layout/16-CONTEXT.md
@.planning/phases/16-sidebar-layout/16-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn-vue sidebar component</name>
  <files>staff/app/components/ui/sidebar/</files>
  <action>
Run the shadcn-vue CLI to add the sidebar component:

```bash
cd staff && npx shadcn-vue@latest add sidebar
```

This will install the sidebar component with all its sub-components:
- Sidebar.vue, SidebarContent.vue, SidebarFooter.vue, SidebarGroup.vue
- SidebarGroupContent.vue, SidebarHeader.vue, SidebarInset.vue
- SidebarMenu.vue, SidebarMenuButton.vue, SidebarMenuItem.vue
- SidebarProvider.vue, SidebarRail.vue, SidebarTrigger.vue
- useSidebar composable

Accept any prompts about overwriting files (there shouldn't be conflicts).
  </action>
  <verify>
Verify the sidebar directory exists and contains the expected files:
```bash
ls staff/app/components/ui/sidebar/
```
Should list multiple .vue files and index.ts.

Verify TypeScript recognizes the exports:
```bash
cd staff && npx nuxi typecheck 2>&1 | head -50
```
No errors related to sidebar imports.
  </verify>
  <done>staff/app/components/ui/sidebar/ directory exists with Sidebar components and useSidebar composable exported from index.ts</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn-vue avatar, dropdown-menu, and tooltip components</name>
  <files>
staff/app/components/ui/avatar/
staff/app/components/ui/dropdown-menu/
staff/app/components/ui/tooltip/
  </files>
  <action>
Run the shadcn-vue CLI to add the remaining components:

```bash
cd staff && npx shadcn-vue@latest add avatar dropdown-menu tooltip
```

This will install:
- avatar: Avatar.vue, AvatarFallback.vue, AvatarImage.vue
- dropdown-menu: DropdownMenu.vue, DropdownMenuContent.vue, DropdownMenuItem.vue, DropdownMenuTrigger.vue, and other sub-components
- tooltip: Tooltip.vue, TooltipContent.vue, TooltipProvider.vue, TooltipTrigger.vue

Accept any prompts. Note: Sheet component is already installed (required by Sidebar for mobile drawer).
  </action>
  <verify>
Verify all directories exist with expected files:
```bash
ls staff/app/components/ui/avatar/
ls staff/app/components/ui/dropdown-menu/
ls staff/app/components/ui/tooltip/
```
Each should have multiple .vue files and index.ts.

Run type check to ensure no conflicts:
```bash
cd staff && npx nuxi typecheck 2>&1 | head -50
```
  </verify>
  <done>Avatar, dropdown-menu, and tooltip component directories exist with all sub-components exported from their respective index.ts files</done>
</task>

</tasks>

<verification>
All four component directories exist and export correctly:
```bash
# Check all directories exist
ls -la staff/app/components/ui/sidebar/
ls -la staff/app/components/ui/avatar/
ls -la staff/app/components/ui/dropdown-menu/
ls -la staff/app/components/ui/tooltip/

# Verify TypeScript compilation
cd staff && npx nuxi typecheck
```
</verification>

<success_criteria>
1. staff/app/components/ui/sidebar/ directory exists with Sidebar, SidebarProvider, SidebarInset, SidebarTrigger, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarHeader, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupContent, SidebarRail, and useSidebar composable
2. staff/app/components/ui/avatar/ directory exists with Avatar, AvatarFallback, AvatarImage components
3. staff/app/components/ui/dropdown-menu/ directory exists with DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem components
4. staff/app/components/ui/tooltip/ directory exists with Tooltip, TooltipTrigger, TooltipContent, TooltipProvider components
5. TypeScript compilation passes (npx nuxi typecheck)
</success_criteria>

<output>
After completion, create `.planning/phases/16-sidebar-layout/16-01-SUMMARY.md`
</output>
