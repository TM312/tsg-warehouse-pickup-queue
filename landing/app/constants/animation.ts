/** Default threshold (0–1) at which IntersectionObserver triggers reveal animations. */
export const REVEAL_THRESHOLD = 0.15

/** Default stagger delay (ms) between child elements in section reveal animations. */
export const DEFAULT_REVEAL_STAGGER_MS = 150

/** Cubic ease-out exponent used in number animation easing function. */
export const EASE_OUT_EXPONENT = 3

/** Precision multiplier for rounding ROI multiplier to one decimal place. */
export const ROI_ROUNDING_PRECISION = 10

/** Scroll distance (px) before the nav bar is considered "scrolled". */
export const NAV_SCROLL_THRESHOLD = 10

/** Stagger delay (ms) between hero mockup row animations. */
export const HERO_ANIMATION_DELAY_S = 0.8

/** Offset (s) for gate-assignment fade-in relative to row animation. */
export const HERO_GATE_DELAY_OFFSET_S = 2
